import React from "react";
import BoardSkeleton from "./index";

export default {
  title: "common/skeletons/BoardSkeleton",
  component: BoardSkeleton,
};

const Template = () => {
  return <BoardSkeleton />;
};

export const Default = Template.bind({});
