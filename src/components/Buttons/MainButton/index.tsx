import React from "react";
import * as S from "./style";

interface Props {
  isBlue?: boolean;
}
function MainButton({ isBlue }: Props) {
  return <S.Container>MainButton</S.Container>;
}

export default MainButton;
