import React from "react";
import CardDetailModalButtons from "./index";

export default {
  title: "components/buttons/CardDetailModalButtons",
  component: CardDetailModalButtons,
};

const Template = () => {
  return <CardDetailModalButtons />;
};

export const Default = Template.bind({});
