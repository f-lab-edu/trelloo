import React from "react";
import LoginPage from ".";

export default {
  title: "Pages/LoginPage",
  component: LoginPage,
  argTypes: {},
};

const Template = () => {
  return <LoginPage />;
};

export const Primary = Template.bind({});
