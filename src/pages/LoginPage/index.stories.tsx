import React from "react";
import LoginPage from ".";

export default {
  title: "Pages/LoginPage",
  component: LoginPage,
};

const Template = () => {
  return <LoginPage />;
};

export const Default = Template.bind({});
